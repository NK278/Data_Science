a = [2, 3, 4, 5, 6, 7]  # Create a list with the given elements
a.append('mango')       # Add 'mango' to the end of the list
a.extend(['banana', 'grapes', 'orange'])  # Add multiple items to the end of the list
a.insert(4, 'apple')    # Insert 'apple' at the 5th position (index 4) in the list
a.pop()                 # Remove the last item from the list in one line
print(a)